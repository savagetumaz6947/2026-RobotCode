package frc.robot.commands;

/**
 * Removed compatibility stub. Use {@link AutoAim} or {@link ShooterAngleAim} instead.
 *
 * <p>This class intentionally throws if instantiated to make usages obvious at runtime and to
 * encourage migrating callers to the new command name. It remains in the repo only to avoid causing
 * compile-time errors in external code that may reference it; remove entirely when callers are
 * migrated.
 */
@Deprecated
public final class ShooterAngleAutoAim {
  private ShooterAngleAutoAim() {
    throw new UnsupportedOperationException(
        "ShooterAngleAutoAim removed - use frc.robot.commands.AutoAim or ShooterAngleAim instead");
  }
}
